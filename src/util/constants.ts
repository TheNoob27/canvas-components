import cssProperties from "../cssProperties"

const Constants = {
  cssProperties,
  supportedProperties: [
    "backgroundColor",
    "display"
  ],
  supportedValues: {
    display: ["block", "inline", "inline-block"]
  }
}

export default Constants